/**
 * Configuração base para chamadas à API do PetMatch.
 *
 * Placeholder do andaime: a implementação dos clients HTTP
 * (autenticação, pets, matches) será adicionada futuramente.
 */

// URL base da API NestJS (backend-api).
export const API_BASE_URL = 'http://localhost:3000';

// URL base do motor de recomendação (recommendation-engine).
export const AI_ENGINE_BASE_URL = 'http://localhost:8000';
